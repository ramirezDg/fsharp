open System
(* printf "Enter the base of the triangle: "

let dataBase = Console.ReadLine()

printfn ""

printf "Enter the height of the triangle: "

let dataHeight = Console.ReadLine()

let calculateTriangleArea b h =
    ((float b) * (float h)) / 2.0

let area = calculateTriangleArea dataBase dataHeight

printfn $"The area of the triangle is: {area}" *)

(* printfn "Juego de la adivinanza"
printfn ""
printfn "Adivina el número del 1 al 10"

let numberInp = Console.ReadLine()

let numberCorrect = 5.0

let rec gameLoop() =
    printf "Ingrese un número: "
    let numberInp = Console.ReadLine()
    if (float numberInp) = numberCorrect then
        printfn "¡Ganaste!"
    else
        printfn "Perdiste. Inténtalo de nuevo."
        gameLoop()

gameLoop() *)

(* printf "Ingrese un número: "
let numberOne = Console.ReadLine()
printf "Ingrese otro número: "
let numberTwo = Console.ReadLine()
printfn ""

if (int numberOne) > (int numberTwo)  then
    printfn $"{numberOne} es mayor que {numberTwo}"
else
    printfn $"{numberTwo} es mayor que {numberOne}" *)
